# Moveable box generator Application

## General Info

This is a react app that generates new boxes using "+" keyboard key & moves those boxes using keyboard keys "A" "S" "D" "W"

## Technologies Used

This Project has been implemented using:

- ReactJs

## Setup

To run this project, install it locally using npm:

```
$ npm install
$ npm start
```
